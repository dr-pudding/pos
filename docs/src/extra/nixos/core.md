In addition to being a prerequisite for the other NixOS modules, the core module provides some features of its own, including basic audio/visual drivers and Catppuccin color defaults.

More significantly, it also provides some additional options for session/login management. It also includes a new command-line utility, `pos`, which provides simple aliases for managing the system. Try running `pos --help` to learn more about what it can do.
